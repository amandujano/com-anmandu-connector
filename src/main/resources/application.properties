
# the name of Camel
camel.main.name = MyCamel

# what to say
greeting = Hello World

# how often to trigger the timer
timer.period = 500

# to automatic shutdown the JVM after a period of time
#camel.main.duration-max-seconds=60
#camel.main.duration-max-messages=100

# add for example: &repeatCount=5 to the timer endpoint to make Camel idle
#camel.main.duration-max-idle-seconds=15

# expose actuator endpoint via HTTP
management.endpoints.web.exposure.include=info,health,camelroutes

# turn on actuator health check
management.endpoint.health.enabled = true

# allow to obtain basic information about camel routes (read only mode)
management.endpoint.camelroutes.enabled = true
management.endpoint.camelroutes.read-only = true

# to configure logging levels
#logging.level.org.springframework = INFO
#logging.level.org.apache.camel.spring.boot = INFO
#logging.level.org.apache.camel.impl = DEBUG
#logging.level.sample.camel = DEBUG

spring.devtools.restart.enabled=false

#camel dataformat
camel.dataformat.jackson.allow-unmarshall-type=true
camel.dataformat.jackson.auto-discover-object-mapper=true
camel.dataformat.jackson.useList=true

camel.component.file.enabled=true